{
  "permissions": {
    "allow": [
      "Bash(ffprobe -v error -show_entries stream=codec_name,codec_type,width,height -of compact overlay.mp4)",
      "Bash(ffprobe -v error -show_entries stream=codec_name,codec_type,width,height -of compact broker.mp4 warden.mp4 navigator.mp4)",
      "Bash(npm run:*)"
    ]
  }
}
